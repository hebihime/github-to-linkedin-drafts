# AGENTS.md

Read `GROK.md` first. It is the source of truth for architecture, scoring, and commit message rules.

When you commit in this repo, follow **Commit messages (required)** in `GROK.md`: Conventional Commits (`feat` / `fix` / `perf` / `refactor` / breaking `!`), picked from the diff, not as decoration. The operator will not add those prefixes themselves.
